NODE_ENV=development
PORT=3000

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=pos_db
DATABASE_USER=pos_user
DATABASE_PASSWORD=pos_password_dev

REDIS_HOST=localhost
REDIS_PORT=6379

# REQUIRED - Application will not start without these
# Use strong, random secrets in production (minimum 32 characters recommended)
JWT_ACCESS_SECRET=your_access_secret_here
JWT_REFRESH_SECRET=your_refresh_secret_here
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# REQUIRED for first-time setup - Initial admin password (minimum 8 characters)
# Change this to a strong password in production before running seed
ADMIN_INITIAL_PASSWORD=change_this_before_production

# CORS Configuration - Comma-separated list of allowed origins
# In production, set this to your frontend domain(s)
# Example: ALLOWED_ORIGINS=https://pos.example.com,https://admin.example.com
ALLOWED_ORIGINS=http://localhost:3001

# Rate Limiting (configured in rateLimit.middleware.ts)
# - API routes: 100 requests per 15 minutes per IP
# - Login endpoint: 5 attempts per 15 minutes per IP
# - Token refresh: 30 requests per 15 minutes per IP
# No configuration needed - limits are hardcoded for security
